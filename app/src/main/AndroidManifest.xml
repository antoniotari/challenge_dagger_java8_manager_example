<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    package="com.antoniotari.challenge"
    xmlns:tools="http://schemas.android.com/tools">

    <uses-permission android:name="android.permission.INTERNET" />

    <application
        android:allowBackup="true"
        android:icon="@mipmap/ic_launcher"
        android:label="@string/app_name"
        android:name="com.antoniotari.challenge.GroceryQuizApplication"
        android:theme="@style/AppTheme"
        tools:replace="android:theme,android:icon,android:label" >

        <activity
            android:name="com.antoniotari.challenge.activities.StartActivity"
            android:label="@string/app_name"
            android:screenOrientation="portrait"
            android:configChanges="fontScale|orientation|screenSize"
            android:windowSoftInputMode="adjustPan" >
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />
                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>

        <activity
            android:name="com.antoniotari.challenge.activities.MainActivity"
            android:label="@string/app_name"
            android:screenOrientation="portrait"
            android:launchMode="singleTask"
            android:configChanges="fontScale|orientation|screenSize"
            android:windowSoftInputMode="adjustPan" >
        </activity>
    </application>

</manifest>
